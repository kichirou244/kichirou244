name: Daily Content Update

on:
  schedule:
    - cron: '30 1 * * *'
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install Dependencies
        run: npm install

      - name: Generate README
        run: npm run gen
        env:
          API_NINJAS_KEY: ${{ secrets.API_NINJAS_KEY }}

      - name: Commit and Push
        run: |
          git config --global user.name "kichirou244"
          git config --global user.email "mrphuc244@gmail.com"
          git add README.md
          git commit -m "docs: daily profile update with new quote" || echo "No changes to commit"
          git push
